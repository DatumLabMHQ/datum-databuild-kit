# {{title}}

**Status:** building (generated by datum-kit on {{today}})
**Repository:** DatumLabMHQ/{{slug}} (dashboard), DatumLabMHQ/datum-models (data)
**Refresh:** hourly by the platform (`scripts/ingest_{{schema}}.py`)
**Owner:** Datum Labs

## What it contains
Describe the entities snapshotted (pools, markets, vaults, assets) and the source ({{url}}).

## Tables it reads from (datum-models)
- `{{schema}}.fct_{{schema}}_daily`: one row per entity per UTC day with value_usd, secondary_usd, rate_pct.
- `ops.sync_runs` (job `{{schema}}.snapshots`), `ops.source_freshness` (source `{{source_id}}`).

## Metrics shown
Name the headline metric and its house definition (see metrics/).

## Known gaps
History starts on the day the product was added.

## How to reproduce a number
`select sum(value_usd) from {{schema}}.fct_{{schema}}_daily where day = :date`, also served as `/api/v1/{{schema}}/daily`.
